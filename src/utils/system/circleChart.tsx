export function extractCircleValue(object: any): any {

    let real = {}

    // Object.keys(object).forEach((key: any) => {
    //     object[key].played.forEach((each: any, order: any) => {

    //     })
    // })

}